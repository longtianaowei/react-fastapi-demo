# React + FastAPI 项目启动说明

本项目由以下部分组成：

- `frontend`：React + TypeScript + Vite
- `backend`：FastAPI + SQLAlchemy + MySQL

## 1. 环境要求

启动前请安装：

- Python 3.11 或更高版本
- [uv](https://docs.astral.sh/uv/)
- Node.js 20.19 或更高版本
- MySQL 8（MySQL 5.7 通常也可运行）

以下命令都从项目根目录执行：

```bash
cd /Users/yutouni/Desktop/code/react-fastapi-demo
```

## 2. 准备 MySQL

先启动 MySQL，然后创建项目使用的数据库：

```sql
CREATE DATABASE demo
    CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;
```

首次运行时，后端会自动创建 `users` 表，不需要手动建表。

## 3. 配置后端环境变量

如果 `backend/.env` 不存在，复制示例文件：

```bash
cp backend/.env.example backend/.env
```

然后根据本机 MySQL 配置修改 `backend/.env`：

```dotenv
MYSQL_USER=root
MYSQL_PASSWORD=你的MySQL密码
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=demo
```

## 4. 安装依赖

安装后端依赖：

```bash
cd backend
uv sync
cd ..
```

安装前端依赖：

```bash
cd frontend
npm ci
cd ..
```

## 5. 启动项目

前端和后端需要分别在两个终端中运行。

终端 1，启动后端：

```bash
cd /Users/yutouni/Desktop/code/react-fastapi-demo/backend
uv run uvicorn app.main:app --reload
```

后端默认地址：

- API：http://localhost:8000
- API 文档：http://localhost:8000/docs
- 测试接口：http://localhost:8000/hello

终端 2，启动前端：

```bash
cd /Users/yutouni/Desktop/code/react-fastapi-demo/frontend
npm run dev
```

浏览器访问：http://localhost:5173

## 6. 停止项目

分别在两个运行中的终端按 `Ctrl+C`。

## 常见问题

### 后端无法连接 MySQL

确认 MySQL 服务已启动、`demo` 数据库已创建，并检查 `backend/.env` 中的用户名、密码和端口。

### 前端请求后端失败

确认后端运行在 `http://localhost:8000`，前端运行在 `http://localhost:5173`。当前项目的 API 地址和跨域配置使用了这两个端口。

### `uv sync` 提示找不到 `src/backend/__init__.py`

确认 `backend/pyproject.toml` 中包含以下配置：

```toml
[tool.uv]
package = false
```
